.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
.idea/
*.iml
.DS_Store
local.properties

# Credentials never belong in the tree. Listed even though none are committed today:
# in a public repo an accidental commit is permanent, and the Sellfox and database
# secrets are read from the environment precisely so they can stay out.
.env
.env.*
*.local.yml

# Built portal, copied in by build-image.sh before the image build.
frontend-dist/
